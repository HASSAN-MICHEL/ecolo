server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Optionnel : gérer les erreurs 404 en renvoyant index.html aussi
    error_page 404 /index.html;
}